# OpenAI / OpenRouter API Configuration
# Option 1: use OpenRouter (recommandé)
#   - Crée un compte sur https://openrouter.ai
#   - Récupère ta clé API et mets-la ci-dessous
OPENROUTER_API_KEY=your_openrouter_api_key_here
# Modèle par défaut côté OpenRouter (tu peux le changer)
OPENROUTER_MODEL=openrouter/openai/gpt-4o-mini

# Option 2: utiliser directement OpenAI (si pas d'OpenRouter)
OPENAI_API_KEY=your_openai_api_key_here
# Modèle OpenAI par défaut
OPENAI_MODEL=gpt-4o-mini

# Etherscan API Configuration (Required for address-based contract analysis)
# Get your free API key at: https://etherscan.io/apis
ETHERSCAN_API_KEY=your_etherscan_api_key_here

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True

# Server Configuration
HOST=0.0.0.0
PORT=5000

# Optional: Database Configuration (if needed later)
# DATABASE_URL=sqlite:///auditor.db 